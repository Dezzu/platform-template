export const environment = {
  production: true,
  appName: 'SaaS Template',
  apiUrl: '/api',
  authUrl: '/api/auth',
  /**
   * Set this to the marketing site's origin before deploying — it is where the cookie
   * banner and the sign-up form send people to read the policy. Left empty the links
   * are simply not rendered, which is a banner without a policy behind it.
   */
  webUrl: '',
};
